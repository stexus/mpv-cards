//import * as ffmpeg from '../ffmpeg'

describe('Audio', () => {
  it('outputs correct timestamps with one line', () => {
    //mpv seek to some set time
    //mpv input b twice
    //
    //const actual_audio_ss: number = 1.0;
    //const actual_audio_to: number = 2.0;

  })
  it('outputs correct timestamps with two lines', () => {

  })
});

describe('Picture', () => {
  it('outputs correct timestamp with one line', () => {

  })
  it('outputs correct timestamp with two lines', () => {

  })
});
